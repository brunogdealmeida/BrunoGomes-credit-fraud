name: fraud_lakehouse
version: "1.0.0"
config-version: 2

profile: datalakehouse

model-paths: ["models"]
test-paths: ["tests"]
macro-paths: ["macros"]

target-path: "target"
clean-targets: ["target", "dbt_packages"]

# dbt is a quality gate only — no models are materialized.
# All tests run against Spark-produced Iceberg tables via Dremio.
models: {}
