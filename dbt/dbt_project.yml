name: fraud_lakehouse
version: "1.0.0"
config-version: 2

profile: datalakehouse

model-paths: ["models"]
test-paths: ["tests"]
analysis-paths: ["analyses"]
macro-paths: ["macros"]

target-path: "target"
clean-targets: ["target", "dbt_packages"]

models:
  fraud_lakehouse:
    silver:
      +materialized: view
      +tags: ["silver", "quality"]
    gold:
      +materialized: view
      +tags: ["gold", "analytics"]
